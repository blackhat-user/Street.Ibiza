import '/React version/app/src/Sass/components/header.css'

const Header = () => {
    return (
        <header class='header__wrapper'>
            
        </header>
    );
}

export default Header;